// فایل تستی برای بررسی ریویوی خودکار — عمداً قوانین CLAUDE.md را نقض می‌کند.
export function BadButton() {
  return (
    <button style={{ background: "#ff0000", color: "#ffffff" }}>
      ثبت نام کنید
    </button>
  );
}
